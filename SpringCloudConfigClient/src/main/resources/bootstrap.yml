spring:
    application:
        name: config-client
    cloud:
        config:
            uri: http://localhost:8888
            username: root
            password: s3cr3t
    profiles:
        active: dev

management:
    security:
        enabled: false